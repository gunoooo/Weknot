<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.weknot_android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE"/>

    <application
        android:allowBackup="true"
        android:icon="@drawable/app_image"
        android:label="@string/app_name"
        android:roundIcon="@drawable/app_image"
        android:supportsRtl="true"
        android:usesCleartextTraffic="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".view.activity.SplashActivity" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".view.activity.LoginActivity" />
        <activity
            android:name=".view.activity.SignUpActivity"/>
        <activity
            android:name=".view.activity.MainActivity"
            android:label="@string/main_title"/>
        <activity
            android:name=".view.activity.FeedWriteActivity"
            android:label="@string/feed_write_title"/>
        <activity
            android:name=".view.activity.ChatActivity"
            android:label="@string/chat_title" />
        <activity
            android:name=".view.activity.ProfileActivity"
            android:label="@string/profile_title" />
        <activity android:name=".view.activity.VideoCallActivity" />
    </application>

</manifest>